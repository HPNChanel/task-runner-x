[project]
name = "taskrunnerx"
version = "0.1.0"
description = "Redis Streams + FastAPI + MySQL task runner"
requires-python = ">=3.10"

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
select = ["E","F","I","B"]
ignore = ["E501"]
